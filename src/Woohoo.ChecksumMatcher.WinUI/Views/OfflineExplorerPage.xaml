<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Woohoo.ChecksumMatcher.WinUI.Views.OfflineExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="using:Woohoo.ChecksumMatcher.WinUI.Converter"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Woohoo.ChecksumMatcher.WinUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Woohoo.ChecksumMatcher.WinUI.ViewModels"
    xmlns:views="using:Woohoo.ChecksumMatcher.WinUI.Views"
    xmlns:vm="using:Woohoo.ChecksumMatcher.WinUI.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converter:IntegerToFileSizeTextConverter x:Key="FileSizeConverter" />

        <DataTemplate x:Key="DiskTemplate" x:DataType="viewmodels:OfflineExplorerDiskViewModel">
            <TextBlock>
                <Run Text="Name: " />
                <Run Text="{x:Bind Name, Mode=OneWay}" />
                <Run Text=" - Label: " />
                <Run Text="{x:Bind Label, Mode=OneWay}" />
                <Run Text=" - Volume Serial: " />
                <Run Text="{x:Bind SerialNumber, Mode=OneWay}" />
            </TextBlock>
        </DataTemplate>

        <DataTemplate x:Key="TreeNoSelectionTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <Grid />
        </DataTemplate>

        <DataTemplate x:Key="TreeFolderTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <TreeViewItem
                AutomationProperties.Name="{x:Bind Name, Mode=OneWay}"
                DoubleTapped="TreeViewItem_DoubleTapped"
                IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                ItemsSource="{x:Bind SortedFolders, Mode=OneWay}"
                KeyUp="TreeViewItem_KeyUp">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE8B7;" />
                    <TextBlock Margin="0,0,10,0" />
                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                </StackPanel>
                <TreeViewItem.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind CollapseAllDescendantsCommand, Mode=OneWay}" Text="Collapse All Descendants" />
                        <MenuFlyoutItem Command="{x:Bind ExpandAllDescendantsCommand, Mode=OneWay}" Text="Expand All Descendants" />
                        <MenuFlyoutSeparator />
                        <MenuFlyoutItem Command="{x:Bind CopyNameToClipboardCommand, Mode=OneWay}" Text="Copy Name" />
                        <MenuFlyoutItem Command="{x:Bind CopyPathToClipboardCommand, Mode=OneWay}" Text="Copy Path" />
                    </MenuFlyout>
                </TreeViewItem.ContextFlyout>
            </TreeViewItem>
        </DataTemplate>

        <DataTemplate x:Key="TreeArchiveTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <TreeViewItem AutomationProperties.Name="{x:Bind Name, Mode=OneWay}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xF012;" />
                    <TextBlock Margin="0,0,10,0" />
                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                </StackPanel>
            </TreeViewItem>
        </DataTemplate>

        <views:OfflineExplorerItemTemplateSelector
            x:Key="TreeTemplateSelector"
            ArchiveTemplate="{StaticResource TreeArchiveTemplate}"
            FolderTemplate="{StaticResource TreeFolderTemplate}" />

        <DataTemplate x:Key="ArchiveListTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind CopyNameToClipboardCommand, Mode=OneWay}" Text="Copy Name" />
                        <MenuFlyoutItem Command="{x:Bind CopyPathToClipboardCommand, Mode=OneWay}" Text="Copy Path" />
                    </MenuFlyout>
                </Grid.ContextFlyout>

                <FontIcon
                    Width="32"
                    Height="32"
                    Margin="8,4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Glyph="&#xF012;" />

                <TextBlock
                    Grid.Column="1"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />

                <TextBlock
                    Grid.Column="2"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    HorizontalTextAlignment="Right"
                    Text="{x:Bind Size, Converter={StaticResource FileSizeConverter}, Mode=OneWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="FileListTemplate" x:DataType="viewmodels:OfflineExplorerFileViewModel">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind CopyNameToClipboardCommand, Mode=OneWay}" Text="Copy Name" />
                        <MenuFlyoutItem Command="{x:Bind CopyPathToClipboardCommand, Mode=OneWay}" Text="Copy Path" />
                    </MenuFlyout>
                </Grid.ContextFlyout>

                <FontIcon
                    Width="32"
                    Height="32"
                    Margin="8,4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Glyph="&#xE8A5;" />

                <TextBlock
                    Grid.Column="1"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />

                <TextBlock
                    Grid.Column="2"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    HorizontalTextAlignment="Right"
                    Text="{x:Bind Size, Converter={StaticResource FileSizeConverter}, Mode=OneWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="FolderListTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind CopyNameToClipboardCommand, Mode=OneWay}" Text="Copy Name" />
                        <MenuFlyoutItem Command="{x:Bind CopyPathToClipboardCommand, Mode=OneWay}" Text="Copy Path" />
                    </MenuFlyout>
                </Grid.ContextFlyout>

                <FontIcon
                    Width="32"
                    Height="32"
                    Margin="8,4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Glyph="&#xE8B7;" />

                <TextBlock
                    Grid.Column="1"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />

                <!--<TextBlock
                    Grid.Column="2"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    HorizontalTextAlignment="Right"
                    Text="{x:Bind Size, Converter={StaticResource FileSizeConverter}, Mode=OneWay}" />-->
            </Grid>
        </DataTemplate>

        <views:OfflineExplorerItemTemplateSelector
            x:Key="ListTemplateSelector"
            ArchiveTemplate="{StaticResource ArchiveListTemplate}"
            FileTemplate="{StaticResource FileListTemplate}"
            FolderTemplate="{StaticResource FolderListTemplate}" />

        <DataTemplate x:Key="SearchResultListViewTemplate" x:DataType="viewmodels:OfflineExplorerSearchResultViewModel">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind OpenContainingFolderCommand, Mode=OneWay}" Text="Open containing folder" />
                    </MenuFlyout>
                </Grid.ContextFlyout>

                <FontIcon
                    Grid.RowSpan="2"
                    Width="32"
                    Height="32"
                    Margin="8,4"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Glyph="&#xE8A5;" />

                <TextBlock
                    Grid.Column="1"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />

                <TextBlock
                    Grid.Column="2"
                    Margin="8,4"
                    VerticalAlignment="Center"
                    HorizontalTextAlignment="Right"
                    Text="{x:Bind Size, Mode=OneWay}" />

                <TextBlock
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="20,0,8,8"
                    VerticalAlignment="Center"
                    FontWeight="Light"
                    Text="{x:Bind Container, Mode=OneWay}" />

            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="FolderDetailsTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <Grid
                Margin="12"
                ColumnSpacing="12"
                RowSpacing="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="80" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Name" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Size" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Created" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Modified" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="CRC32" />
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="MD5" />
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SHA1" />
                <TextBlock
                    Grid.Row="7"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SHA256" />

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Size, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Created, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Modified, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="N/A" />
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="N/A" />
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="N/A" />
                <TextBlock
                    Grid.Row="7"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="N/A" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="ArchiveDetailsTemplate" x:DataType="viewmodels:OfflineExplorerFolderViewModel">
            <Grid
                Margin="12"
                ColumnSpacing="12"
                RowSpacing="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="80" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Name" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Size" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Created" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Modified" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="CRC32" />
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="MD5" />
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SHA1" />
                <TextBlock
                    Grid.Row="7"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SHA256" />

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Size, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Created, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Modified, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind CRC32, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind MD5, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind SHA1, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="7"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind SHA256, Mode=OneWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="FileDetailsTemplate" x:DataType="viewmodels:OfflineExplorerFileViewModel">
            <Grid
                Margin="12"
                ColumnSpacing="12"
                RowSpacing="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="80" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Name" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Size" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Created" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="Modified" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="CRC32" />
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="MD5" />
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SHA1" />
                <TextBlock
                    Grid.Row="7"
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="SHA256" />

                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Name, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="1"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Size, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="2"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Created, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="3"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind Modified, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="4"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind CRC32, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind MD5, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind SHA1, Mode=OneWay}" />
                <TextBlock
                    Grid.Row="7"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind SHA256, Mode=OneWay}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="NoDetailsTemplate">
            <TextBlock
                Margin="12"
                HorizontalAlignment="Center"
                Style="{ThemeResource SubheaderTextBlockStyle}"
                Text="No file selected" />
        </DataTemplate>

        <views:OfflineExplorerItemTemplateSelector
            x:Key="DetailsTemplateSelector"
            ArchiveTemplate="{StaticResource ArchiveDetailsTemplate}"
            FileTemplate="{StaticResource FileDetailsTemplate}"
            FolderTemplate="{StaticResource FolderDetailsTemplate}"
            NoSelectionTemplate="{StaticResource NoDetailsTemplate}" />

    </Page.Resources>

    <Grid>
        <Grid>
            <StackPanel
                VerticalAlignment="Center"
                Orientation="Vertical"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading}" />
                <TextBlock
                    Margin="20"
                    HorizontalTextAlignment="Center"
                    Style="{ThemeResource TitleTextBlockStyle}"
                    Text="Loading..." />
            </StackPanel>
        </Grid>

        <Grid
            x:Name="ContentArea"
            ColumnSpacing="12"
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ComboBox
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Stretch"
                ItemTemplate="{StaticResource DiskTemplate}"
                ItemsSource="{x:Bind ViewModel.Indexes, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedDisk, Mode=TwoWay}" />
            <ScrollViewer
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,12"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{ThemeResource ControlCornerRadius}">
                <TreeView
                    ItemTemplateSelector="{StaticResource TreeTemplateSelector}"
                    ItemsSource="{x:Bind ViewModel.SelectedDisk.RootFolders, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedFolder, Mode=TwoWay}" />
            </ScrollViewer>

            <Grid
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Breadcrumbs for the path  -->
                <Border
                    Grid.Row="0"
                    Padding="4,2,4,6"
                    HorizontalAlignment="Stretch"
                    Background="{ThemeResource ControlFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{ThemeResource ControlCornerRadius}">
                    <BreadcrumbBar ItemClicked="BreadcrumbBar_ItemClicked" ItemsSource="{x:Bind ViewModel.SelectedFolder.Breadcrumbs, Mode=OneWay}">
                        <BreadcrumbBar.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:OfflineExplorerBreadcrumbViewModel">
                                <BreadcrumbBarItem AutomationProperties.Name="{Binding Name}" Content="{Binding}">
                                    <BreadcrumbBarItem.ContentTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="0,-2,0,0" Text="{Binding Name}" />
                                        </DataTemplate>
                                    </BreadcrumbBarItem.ContentTemplate>
                                </BreadcrumbBarItem>
                            </DataTemplate>
                        </BreadcrumbBar.ItemTemplate>
                    </BreadcrumbBar>
                </Border>

                <!--  Search Bar  -->
                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Text="Search" />
                    <TextBox
                        Grid.Column="1"
                        Margin="{StaticResource SmallLeftMargin}"
                        VerticalAlignment="Center"
                        PlaceholderText="Search for file name, size or checksum in folder and subfolders"
                        Text="{x:Bind ViewModel.SelectedFolder.FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        Grid.Column="2"
                        MinWidth="90"
                        Margin="{StaticResource SmallLeftMargin}"
                        VerticalAlignment="Center"
                        HorizontalTextAlignment="Right"
                        Visibility="{x:Bind ViewModel.SelectedFolder.SearchResultsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}">
                        <Run Text="{x:Bind ViewModel.SelectedFolder.FoldersAndFiles.Count, Mode=OneWay}" />
                        <Run Text=" items" />
                    </TextBlock>
                    <TextBlock
                        Grid.Column="2"
                        MinWidth="90"
                        Margin="{StaticResource SmallLeftMargin}"
                        VerticalAlignment="Center"
                        HorizontalTextAlignment="Right"
                        Visibility="{x:Bind ViewModel.SelectedFolder.SearchResultsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Run Text="{x:Bind ViewModel.SelectedFolder.SearchResults.Count, Mode=OneWay}" />
                        <Run Text=" results" />
                    </TextBlock>
                </Grid>

                <ListView
                    x:Name="FoldersAndFilesListView"
                    Grid.Row="2"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    DoubleTapped="ListView_DoubleTapped"
                    ItemTemplateSelector="{StaticResource ListTemplateSelector}"
                    ItemsSource="{x:Bind ViewModel.SelectedFolder.SortedFoldersAndFiles, Mode=OneWay}"
                    KeyUp="FoldersAndFilesListView_KeyUp"
                    SelectedItem="{x:Bind ViewModel.SelectedFile, Mode=TwoWay}"
                    SelectionChanged="FoldersAndFilesListView_SelectionChanged"
                    SelectionMode="Single"
                    ShowsScrollingPlaceholders="True"
                    Visibility="{x:Bind ViewModel.SelectedFolder.SearchResultsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}" />

                <ListView
                    x:Name="SearchResultsListView"
                    Grid.Row="2"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    ItemTemplate="{StaticResource SearchResultListViewTemplate}"
                    ItemsSource="{x:Bind ViewModel.SelectedFolder.SearchResults, Mode=OneWay}"
                    SelectionMode="Single"
                    ShowsScrollingPlaceholders="True"
                    Visibility="{x:Bind ViewModel.SelectedFolder.SearchResultsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

                <!--  Details pane  -->
                <Border
                    Grid.Row="3"
                    MinHeight="170"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Stretch"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    Visibility="{x:Bind ViewModel.SelectedFolder.SearchResultsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True}">

                    <ContentControl
                        HorizontalAlignment="Stretch"
                        Content="{x:Bind ViewModel.SelectedFile, Mode=OneWay}"
                        ContentTemplateSelector="{StaticResource DetailsTemplateSelector}" />
                </Border>
            </Grid>

        </Grid>
    </Grid>
</Page>
